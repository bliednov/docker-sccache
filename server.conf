cache_dir = "/tmp/toolchains"
public_addr = "0.0.0.0:10501"
scheduler_url = "$SHEDULER_ADDR:10600"

[builder]
type = "overlay"
build_dir = "/tmp/build"
bwrap_path = "/usr/bin/bwrap"

[scheduler_auth]
type = "token"
token = "$TOKEN"
